# Server Configuration
PORT=8201
NODE_ENV=production

# PostgreSQL Connection
POSTGRESQL_URL=postgresql://atomiks:PASSWORD@postgres:5432/atomiks?sslmode=disable

# Browser Configuration
HEADLESS=true
BROWSER_TIMEOUT=30000

# Session Management (SOPS)
SESSION_SOPS_KEY_PATH=/secrets/ultramiks-session.enc

# Logging
LOG_LEVEL=info
